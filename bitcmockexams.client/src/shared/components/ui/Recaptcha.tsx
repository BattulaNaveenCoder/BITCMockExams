// Deprecated v2 reCAPTCHA component placeholder. Kept to avoid import errors if referenced.
// Use v3 helper from `@shared/utils/recaptchaV3` instead.
export default function Recaptcha() { return null; }
